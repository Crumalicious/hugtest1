# HugTest1
A Flask-based web application for text and image generation using AI models.

## Features
- Text generation with GPT models
- Expandable with Stable Diffusion for image generation

## Setup
1. Clone the repository.
2. Create a virtual environment:
   ```bash
   python3 -m venv venv
   ```
3. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
4. Run the app:
   ```bash
   python app.py
   ```

## License
MIT License
